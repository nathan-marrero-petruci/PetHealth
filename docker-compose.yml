services:
  api:
    build:
      context: .
      dockerfile: backend/Api/Dockerfile
    env_file: .env
    ports:
      - "8091:8080"
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "8092:80"
    restart: unless-stopped
